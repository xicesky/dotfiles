#!/bin/bash

if [[ -z "$1" ]] ; then
    echo "Usage: $0 <VARIABLENAME>" 1>&2
    exit 1
fi
powershell.exe -Command 'echo "$env:'"$1"'"'

# Without powershell: This works almost as well, but prints bullshit if the variable is not set
# cmd.exe /c echo "%$1%" 2>/dev/null

